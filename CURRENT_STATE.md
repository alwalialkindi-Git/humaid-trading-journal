# CURRENT_STATE

_Last updated: 2026-07-16 (D4 COMPLETE — live `beb195d`, Vercel READY, authenticated production smoke test PASSED 2026-07-15/16)_

## Production

- **URL:** https://humaid-trading-journal.vercel.app (Vercel, team `alwali`, project `humaid-trading-journal`, auto-deploys `main`)
- **Latest commit on `main` / deployed:** `beb195d` — "D4 Dashboard: 60-second review" (Vercel READY, alias verified serving this commit)
- Supabase: MVP schema (001) + ledger migration (`002_ledger.sql`) **applied to live**; `SUPABASE_SERVICE_ROLE_KEY` + market-data env set on Vercel.

## Completed

- **MVP** (`ce7d916`) — 14-page journal/portfolio/zakat app on legacy tables.
- **M1 Phase 1 — ledger schema** (`3c2ead9`): exchanges/assets/asset_overrides/portfolios/brokers/transactions/journal_annotations/positions, RLS, signup trigger; PGlite-validated.
- **M1 Phase 2 — position engine** (same commit): pure average-cost engine, trial-replay ordering, no-shorts; 22+ tests.
- **M1 Phase 3 — market data layer** (`03201d2`): provider abstraction (Yahoo default, Mock fallback, TwelveData/EODHD stubs), UAE search rules incl. ADIB-Egypt guard, `/api/market/*`.
- **M1 Phase 4 — service layer** (`768d44f`): repository pattern, trial-recompute validation, admin-key boundary; 57 tests.
- **M1 Phase 5 — "The Loop"** (`aa7307e`): Add Transaction (8 types), asset search + custom UAE assets, sell preview ≡ engine, brokers, ledger-true Portfolio (Positions/Cash/History), redirects from old pages; 60 tests. **Live-smoke-tested by owner.**
- **Design constitution** (uncommitted until D1a commit): PRD, M1 design doc, Phase 5 UX review committed earlier; Design Sprint 1 (amended), Design Council Final Review (CIO 5 conditions + Irreplaceability Doctrine), AMANAH design system — approved and **frozen**.

## Current phase

**D4 — Dashboard "60-second review": COMPLETE — LIVE IN PRODUCTION (`beb195d`, pushed 2026-07-15, Vercel READY; authenticated production smoke test PASSED — agent-driven, real session, all owner-checklist items; profile/localStorage test state fully restored; NO code defects).** Scope per sprint §28/§9, fused with milestone Phase 6 (page was already ledger-native since Bug-3; D4 is the full layout): (1) **WealthStrip** hero — display-currency total in `figure-xl` from the SAME `getWealthSummary` rows (currency law intact: ≈ secondary, native truth line, missing-FX excluded and named), plus the GLOBAL period selector (D/W/M/YTD/1Y, localStorage `amanah:period`, FinTable-density store pattern) whose change/sparkline slots honestly render "insufficient history" until the M2 daily valuation series (CIO condition 1 — never an approximation; settle-on-load skipped per motion §26). Tripwire updated: dashboard must contain `getWealthSummary` + `WealthStrip` (Wealth keeps SummaryCards). (2) **AttentionQueue** (`lib/attention.ts` pure + tested, `components/dashboard/attention-queue.tsx`) — replaces every banner stack (hawl banner, NegativeCashNotice, shariah card folded in); severity tiers obligation > integrity > freshness > housekeeping; items: hawl ≤30d, purification owed (accrued−paid from ledger), non-compliant, unscreened, negative cash, stale prices (>24h, manual excluded), unpriced; max 5 + "view all"; snooze 7d in localStorage EXCEPT obligations (never snoozable, brass ◆); empty state "All quiet, alhamdulillah." (3) **ExposureBand** — SegmentBar extracted from AllocationBar (Wealth API unchanged), dimension toggle Asset · Class · Currency (account grouping honestly deferred to M4, labels M5.5), top-3 concentration, liquidity split listed/unlisted (◇A6; `is_listed` added to HoldingView), currency split, compliance shield summary — ALL from the one `normalizeDisplayValues` base (§4.9). (4) **FlowsRow** — cash per currency (shared rows), income (dividends MTD/YTD UTC-calendar + purification owed ◆ from `lib/dashboard.ts` incomeSummary, pure + tested), zakat tile (brass; hawl countdown real, accrual estimate honestly pending until M4 engine). (5) **RecentActivity** restyled to the D2 timeline glyph language (sacred types ◆) + **ledger-native insights** (`generateLedgerInsights`: concentration >35%, strongest/weakest ≥±10%, income YTD; max 3; InsightCards re-tokened for dual themes, `columns={1}` seat). 164 tests (＋21: dashboard math 12, attention 9). typecheck/lint/build ✔; local boot: landing 200, /dashboard 500 = known missing-Supabase-env condition (thrown in createClient before D4 code; no local credentials by design).

**D3 — Transaction flow: IMPLEMENTED (local commit, not pushed; awaiting owner review → push → owner's authenticated production pass).** Scope per sprint §28/§11, logic untouched (draft-guard, sell preview ≡ engine, ADIB warning flow = regression baseline, all kept): (1) **Dialog compaction** — type switcher is ONE segmented row (Buy · Sell · Div · Cash ▾ · Obligation ▾; menu segments show the chosen subtype; radiogroup ←/→/Home/End, sell segment disabled without open positions); dialog 560px desktop / bottom-sheet on mobile; sacred copy now on brass (`sacred`/`sacred-surface` tokens) with ◆. (2) **TicketLine** (`lib/transactions/ticket.ts` pure + tested, `components/transactions/ticket-line.tsx`) — live sentence above the footer ("Buy 1,000 EMAAR @ 12.10 — 12,120.00 AED incl. 20.00 AED fees · Personal / IBKR · 8 Jul"), AMANAH §4 formatting, sell P&L preview rides it (same engine export as before; old preview box removed, not duplicated); **the same builder's text becomes the success toast**. (3) **Save-settle moment** — `lib/transactions/settle.ts` store (10s freshness window, tested); dialog marks the written row id; FinTable gained `highlightKey` (+ mobileCard meta) and Activity settles the row with the 2s `animate-settle` verb, then consumes the flag. (4) **Keyboard completeness** — global `N` opens Add Transaction (never while typing/dialog open), ⌘/Ctrl+Enter submits the form, Escape clears open search results before reaching the close guard, segmented-row roving radio. (5) **Asset-search row alignment** — results table-aligned (shield placeholder [dashed not-screened, sr-only label] / symbol / name / exchange / country·ccy grid); warned-row amber + confirm step logic unchanged. 143 tests (＋18: ticket 12, settle 6). typecheck/lint/build ✔. Authenticated dialog pass = owner's production check post-push (no local credentials by design).

**D2 — Wealth redesign: COMPLETE — LIVE IN PRODUCTION (`a688a4d` + fixes `892bb24`, pushed 2026-07-14; authenticated production pass ✔: weights ≡ AllocationBar, FX-normalized value sort, no hydration error, no currency flip, Accounts/Cash/Activity verified).** FinTable flagship (`ui/fin-table.tsx` + pure `lib/fin-table.ts`): closed column-type set, single-column sort (nulls last), density Comfortable 44 / Compact 32 remembered per table (useSyncExternalStore + localStorage), sticky header, per-currency footer aggregates (never mixed, §4.9), month group headers, declared mobile card transform, export seat (disabled until M4). Positions migrated onto it — every figure through the Figure primitive; price/value carry provenance (source · as-of · actor "You" on manual · derivation qty×price). AllocationBar (top-8 + Other, chart tokens, display-ccy via peg FX, unpriced/excluded named). StatBlock primitive; drawer restyled to three zones (identity / stat grid / ledger timeline with glyphs). NEW **Accounts tab** (per-broker ledger facts: recorded count, last activity, currencies; per-account holdings honestly deferred to M4). NEW **Cash statement** (`getCashStatement` service read model): per-currency opening → signed events with running balance → closing, engine replay order via `compareTransactions`, **closing ≡ engine balance tested**; sacred payments marked ◆. Tabs now Positions · Accounts · Cash · Activity (legacy `?tab=history` redirects). 119 tests (＋15: fin-table logic 9, cash statement 6). typecheck/lint/build ✔. Local boot verified (landing 200); authenticated Wealth pass = owner's production check post-push (no local credentials by design). Figma reference: system file `Thz2JywmZei5FKdrgT8Td0` (D-018 step-4 frames); B v3 exploration parked unratified in the Lab file — no AMANAH amendments.


**D2 SMOKE-TEST FIXES — COMPLETE, LIVE (`892bb24`).** Owner's authenticated production pass (2026-07-14) surfaced three issues, all fixed: (1) Weight column computed from mixed native currencies (positions-service summed AED+USD raw) — allocation_percent removed from the service; weights now come from `normalizeDisplayValues` (lib/fin-table, pure, FX-normalized via peg provider), ONE base shared by the Weight column, drawer Weight stat, Value sorting, and AllocationBar (identical percentages by test); missing-rate/unpriced holdings excluded BY NAME. (2) Value sort now ranks by FX-normalized display value (native display unchanged; nulls last). (3) React #418 hydration mismatch: timestamps formatted machine-local (server UTC ≠ client GST) — all §4.8 timestamp formatting now uses fixed DISPLAY_TIME_ZONE "Asia/Dubai"; calendar dates/month labels format in UTC. AED/USD preference unaffected (profile-stored, server-rendered). 125 tests (＋6). typecheck/lint/build ✔. Verified in production 2026-07-14.

**D1a — LIVE in production** (`feae5bb`, verified: tokens/dual themes/boot script on prod; owner's authenticated pass ✔).

**D1b — LIVE in production** (`cd8b2c2`; owner's authenticated pass surfaced the regressions below).

**FINANCIAL-TRUTH FIXES (Bugs 1–4) — COMPLETE (local commit, not pushed; awaiting owner review; D2 remains paused).** Draft-guard on the transaction dialog (dirty close → confirm; failures preserve fields; success = exact "Recorded — portfolio figures updated." + revalidation of /portfolio + /dashboard); Wealth header replaced by six shared summary cards (total/market/cash/basis/unrealized/realized) with the tested equations; Dashboard rebuilt on the SAME `getWealthSummary` read model (no legacy trades/holdings/dividends reads — tripwire-tested; legacy banner removed; zakat = honest pending state); dual-currency display (profile-stored USD/AED preference, switcher on both pages, ≈ secondary equivalents, peg FX provider 3.6725 with provenance, missing-rate exclusion). 104 tests.

**D1b delivery notes (superseded heading):** Delivered: grouped nav shell (Council IA — OVERVIEW/WEALTH/THINKING/PURITY◆/UNDERSTAND; labels Wealth·Journal·Screener·Insights·Zakat & Purify, routes unchanged; shared `nav-config.ts` single source), ⌘K command palette (hand-rolled on Dialog, ARIA combobox, Actions + Navigate, opens the same TransactionDialog), mobile bottom tab bar (Home·Wealth·+·Zakat·More sheet; FAB retired; safe-area; toaster raised above bar), compliance ShieldBadge system (filled/half/slashed/dashed-outline shapes + labels — never color-alone; swapped into ComplianceBadge + Positions), glossary registry (`lib/glossary.ts`, 15 terms) + GlossaryTerm popover (wired: sell avg-cost, dividend purification). THINKING houses Journal+Calendar until Research lands (M5.5). Next: owner review of D1b → push → **D2 Wealth redesign**.

## Parking Lot

Deferred by owner decision — do not implement until explicitly scheduled:

- **Unify FX aggregation rounding order across summary and exposure views.** (D4 smoke test, 2026-07-16: exposure-band ≈ total 98,406.68 AED vs Market value card 98,406.69 AED — per-row round2-then-sum vs convert-currency-totals; both ≈-marked, same normalized base; 1-cent display divergence only.)

## Known limitations

- **Two worlds until Phase 8 migration:** Dashboard, Trades, Analytics, Calendar, Watchlist, Shariah, and the Zakat prefill still read **legacy MVP tables**; only Portfolio is ledger-true. Legacy-data notice shown on Dashboard.
- No FX conversion (totals per currency; `≈` conversions arrive M2).
- No daily valuation series yet → **no performance figures permitted** (CIO condition 1).
- Shariah screening engine not implemented (M3) — all badges honestly "Not screened" (or user override).
- Broker import + reconciliation not implemented (M4).
- ADX has no automated provider (manual_custom tier; paid-provider validation in M2).
- Prices refresh on demand only (cron in M2). Yahoo is unofficial — degrades to cached + mock.
- Dark theme (landing page + legacy pages have hardcoded light tints — acceptable until their redesign phases).
- Account deletion is a placeholder (M5).

## Next objective

**D4 pushed and LIVE (`beb195d`, Vercel READY 2026-07-15). Authenticated production smoke test (agent-driven, real session): PASSED** — no Server Action/hydration/console errors (only the known Chrome-extension "message channel closed" artifact); WealthStrip ≡ Wealth page EXACT (6,511.81 AED / ≈1,773.13 USD / native −450.00 AED · 1,895.66 USD, byte-identical across pages); AED↔USD switch swaps primary/secondary exactly and was restored to AED; D/W/M/YTD/1Y selector persists (`amanah:period`) and every period honestly renders "insufficient history"; attention queue live-correct (items tracked the ledger AS THE OWNER ADDED POSITIONS mid-test: 3→8 holdings; snooze hid a freshness item with "1 snoozed" + 7-day expiry, restored; hawl obligation [temporarily set 2025-07-25 in Settings, then cleared — profile restored to null] ranked FIRST, brass ◆, NO snooze control while all other rows have one; zakat tile showed the real 9-day countdown with estimate still honestly pending); exposure toggles verified (Asset/Class="Stocks 100%"/Currency, bar ≡ top-3 ≡ currency split); flows cash ≡ read model, income honest ("No dividends recorded yet"); recent activity = 5 ledger rows in replay order incl. the owner's brand-new buys; insights ledger-native ≤3 (concentration card correctly disappeared when the owner's new positions diluted BABA below 35%); Add Transaction regression pass (N opens dialog, segmented row, mouse-clickable Cash▾ menu [D3 fix], first-Escape clears search, dirty-Escape draft guard → Discard, zero ledger writes). **One observation, not a defect:** exposure-band ≈ total (98,406.68 AED) differs from the Market value card (98,406.69 AED) by 1 cent — per-row round2-then-sum vs convert-currency-totals aggregation; both ≈-marked, same base. All test state restored (hawl null, currency AED, snoozes/period cleared). **D4 COMPLETE.** Next: **D5 — Islamic finance center** per sprint §28 — not started; awaiting owner go-ahead.
